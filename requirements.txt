# GUI相關
tk==8.6.12  # Tkinter，Python的標準GUI工具包

# 數據處理和視覺化
matplotlib==3.7.1  # 用於創建視覺化圖表
numpy==1.24.3  # 用於數值計算和數組操作
pandas==2.0.1  # 用於數據分析和處理

# 文件處理
Pillow==9.5.0  # 用於圖像處理和生成
reportlab==4.0.0  # 用於生成PDF報告

# 加密和安全
cryptography==40.0.2  # 用於敏感數據加密
bcrypt==4.0.1  # 密碼加密

# 數據驗證和存儲
jsonschema==4.17.3  # 用於JSON數據驗證
pyyaml==6.0  # 用於YAML格式的配置文件

# 日誌處理
python-json-logger==2.0.7  # 用於生成結構化的JSON格式日誌

# 開發和測試工具 (可選，開發環境使用)
pytest==7.3.1  # 用於單元測試
black==23.3.0  # 代碼格式化工具
pylint==2.17.4  # 代碼質量檢查工具